@page
@model RazorPagesMovie.Pages.Movies.IndexModel
@using Microsoft.AspNetCore.Identity


    <h1 class="text-center mb-4">🎬 Movie Gallery</h1>

<div class="row">
    @foreach (var movie in Model.Movie)
    {
        var jpg = $"/images/movies/{movie.Id}.jpg";
        var png = $"/images/movies/{movie.Id}.png";
        var imgPath = System.IO.File.Exists("wwwroot" + jpg) ? jpg :
                      System.IO.File.Exists("wwwroot" + png) ? png : "/images/noimage.png";

        <div class="col-md-3 mb-4">
            <div class="card shadow-sm">
                <img src="@imgPath" class="card-img-top" style="height:320px; object-fit:cover;" />
                <div class="card-body text-center">
                    <h5>@movie.Title</h5>
                    <p>@movie.Genre</p>

                    <a class="btn btn-primary btn-sm" href="./Details?id=@movie.Id">View</a>

                    @if (User.Identity.IsAuthenticated)
                    {
                        <a class="btn btn-warning btn-sm" href="./Edit?id=@movie.Id">Edit</a>
                        <a class="btn btn-danger btn-sm" href="./Delete?id=@movie.Id">Delete</a>
                    }

                   
                </div>
            </div>
        </div>
    }
</div>

@{
    ViewData["Title"] = "Index";
}


<div class="text-center mt-5">
    <p class="lead">Your best movies are here</p>

    <a class="btn btn-primary btn-lg mt-4" asp-page="/Movies/Index">
        🎬 Movies
    </a>
</div>


<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>

<form>
    <p>
        <select asp-for="MovieGenre" asp-items="Model.Genres">
            <option value="">All</option>
        </select>
        <label>Title: <input type="text" asp-for="SearchString" /></label>
        <input type="submit" value="Filter" />
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].ReleaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].Genre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].Rating)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Movie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReleaseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
